import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 30,
  },
  title: {
    paddingLeft: 15,
    fontSize: 25,
  },
  row: {
    paddingTop: 10,
    justifyContent: 'space-around',
    flexDirection: 'row',
  },
});
