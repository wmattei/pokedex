import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  surface: {
    borderRadius: 10,
    width: 160,
    height: 160,
    marginTop: 4,
    marginBottom: 4,
    alignSelf: 'center',
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    marginTop: 10,
    fontSize: 24,
  },
});
