import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  nameContainer: {
    height: 60,
  },
  name: {
    fontSize: 30,
    marginTop: 60,
    textAlign: 'center',
    fontWeight: 'bold',
  },
});
