import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 50,
    // padding: 8,
    // justifyContent: 'center',
    // alignItems: 'center',
  },
  title: {
    padding: 10,
    fontSize: 30,
  },
});
