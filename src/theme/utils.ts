import { StyleSheet } from 'react-native';

export const fullWidth = StyleSheet.create({
  main: {
    width: '100%',
  },
});

export const fullHeight = StyleSheet.create({
  main: {
    height: '100%',
  },
});
